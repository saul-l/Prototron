/* Run Generator Component handles thing related to generating a run
 * 
 * -Determines which enemy types are chosen for a run and what they cost
 * -Determines power ups and weapons and their probabilities
 * -Creates pools for weapons and enemies
 * 
 * This is work in progress. At the moment only hold weapon pool.
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RunGeneratorComponent : MonoBehaviour
{
    Pool powerUpPool;
    Pool weaponPool;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
